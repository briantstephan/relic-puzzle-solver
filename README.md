# Relic Puzzle Solver

For solving difficult relic puzzles. 

Puzzles are solved client-side and have a timeout, so slower computers may produce less optimal solutions.

Uses React, TypeScript, Vite, and pnpm.

## Running locally

```bash
pnpm i
pnpm run dev
```
